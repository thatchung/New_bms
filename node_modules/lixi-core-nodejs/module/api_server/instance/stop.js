/** tắt server */

module.exports = server =>
    (server.stop = function() {
        if (!server.koa_instance) return;

        server.koa_instance.close();
        delete server.koa_instance;
    });
