let Info = require('../../../info');
let Moment = require('../../moment/_index');

module.exports = async function({ aliases, document }) {
    // #region [lấy thông tin từ bitbucket]

    let auth;
    let repository;
    let repository_last_commit;

    let tasks = [];

    // #endregion

    document.info.core_framework = Info;
    document.info.name = repository ? repository.name : 'document';
    document.info.description = repository ? repository.description : undefined;
    document.info.update_datetime = Moment().valueOf();
};
