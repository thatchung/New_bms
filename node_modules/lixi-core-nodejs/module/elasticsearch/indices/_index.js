/** các hàm của class */

module.exports = ({ elastic }) => {
    elastic.indices = {};

    require('./create_table')({ elastic });
    require('./drop_table')({ elastic });
    require('./put_mapping')({ elastic });
};
