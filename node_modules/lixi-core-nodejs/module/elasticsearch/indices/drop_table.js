/** xóa bảng */

module.exports = ({ elastic }) =>
    (elastic.indices.dropTable = async function(table_name) {
        return await elastic.client.indices.delete({
            index: elastic.options.prefix + table_name
        });
    });
