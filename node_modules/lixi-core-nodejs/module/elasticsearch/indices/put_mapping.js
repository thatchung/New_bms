/** cập nhật mapping của bảng */

module.exports = ({ elastic }) =>
    (elastic.indices.putMapping = async function({ table_name, mappings }) {
        return await elastic.client.indices.putMapping({
            index: elastic.options.prefix + table_name,
            type: table_name,
            body: {
                properties: mappings
            }
        });
    });
