/** bắt đầu câu truy vấn */

module.exports = elastic =>
    (elastic.query = function() {
        let query = { _body: {} };

        // cài đặt các method cho đối tượng elastic query
        require('../query/_index')({ elastic, query });

        return query;
    });
