/** các hàm của class */

module.exports = ({ elastic, query }) => {
    require('./index')({ elastic, query });
    require('./type')({ elastic, query });

    require('./id')({ elastic, query });
    require('./clear_id')({ elastic, query });

    require('./filter_must')({ elastic, query });
    require('./clear_filter_must')({ elastic, query });

    require('./filter_should')({ elastic, query });
    require('./clear_filter_should')({ elastic, query });

    require('./filter_must_not')({ elastic, query });
    require('./clear_filter_must_not')({ elastic, query });

    require('./aggregations')({ elastic, query });
    require('./clear_aggregations')({ elastic, query });

    require('./sort')({ elastic, query });
    require('./clear_sort')({ elastic, query });

    require('./search_after')({ elastic, query });
    require('./clear_search_after')({ elastic, query });

    require('./from')({ elastic, query });
    require('./size')({ elastic, query });
    require('./paging')({ elastic, query });

    require('./get')({ elastic, query });
    require('./exists')({ elastic, query });
    require('./count')({ elastic, query });
    require('./search')({ elastic, query });
    require('./aggregate')({ elastic, query });

    require('./bulk')({ elastic, query });
    require('./insert')({ elastic, query });
    require('./update')({ elastic, query });
    require('./delete')({ elastic, query });
    require('./delete_by_query')({ elastic, query });
};
