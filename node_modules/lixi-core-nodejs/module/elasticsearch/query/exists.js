/** thực hiện truy vấn exists */

module.exports = ({ elastic, query }) =>
    (query.exists = async function() {
        return (
            await elastic.client.exists({
                index: query._body.index,
                type: query._body.type,
                id: query._body.id
            })
        ).body;
    });
