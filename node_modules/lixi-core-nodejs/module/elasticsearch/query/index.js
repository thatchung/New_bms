/** chọn index để thao tác */

module.exports = ({ elastic, query }) =>
    (query.index = function(index) {
        query._body.index =
            elastic.options.prefix + index + elastic.options.postfix;
        if (!query._body.type) query._body.type = index;

        return query;
    });
