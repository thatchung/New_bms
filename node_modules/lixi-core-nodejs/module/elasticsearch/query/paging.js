/** đặt size */

module.exports = ({ elastic, query }) =>
    (query.paging = function(page, page_size) {
        query._body.from = (page - 1) * page_size;
        query._body.size = page_size;

        return query;
    });
