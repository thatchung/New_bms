/** đặt search after */

module.exports = ({ elastic, query }) =>
    (query.searchAfter = async function(search_after) {
        query._body.search_after = Array.isArray(search_after)
            ? search_after
            : [search_after];

        return query;
    });
