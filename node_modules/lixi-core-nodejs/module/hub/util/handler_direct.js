module.exports = async function(param) {
    // #region [chuẩn hóa param]

    if (param === undefined || param === null) param = {};
    if (typeof param !== 'object') throw Error('"param" must be object');

    // #endregion

    return await require(this.file)(param);
};
