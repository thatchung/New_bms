/** thư viện joi, chuyên validate field và schema */

let Joi = require('joi');

Joi = require('./type/string')(Joi);
Joi = require('./type/phone_number')(Joi);
Joi = require('./type/timestamp')(Joi);

require('./method/validate_value')(Joi);

module.exports = Joi;
