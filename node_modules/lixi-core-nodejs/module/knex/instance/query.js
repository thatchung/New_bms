/** tạo câu truy vấn */

module.exports = knex =>
    (knex.query = function() {
        let query = knex.client.queryBuilder();

        // mở rộng knex query
        require('../query/_index')({ knex, query });

        return query;
    });
