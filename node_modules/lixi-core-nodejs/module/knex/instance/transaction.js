/** tạo transaction truy vấn */

module.exports = knex =>
    (knex.transaction = function(handler) {
        return knex.client.transaction(async trx => {
            trx.options = knex.options;
            require('./_index')(trx);

            return await handler(trx);
        });
    });
