/** cài đặt elasticsearch
 * @param {Elasticsearch} elasticsearch elasticsearch được tích hợp
 */

module.exports = knex =>
    (knex.useElasticsearch = function({ elasticsearch } = {}) {
        knex.options.elasticsearch = elasticsearch;
    });
