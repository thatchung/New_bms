/** mở rộng, overwrite các hàm của knex */

module.exports = ({ knex, query }) => {
    require('./insert_or_update')({ knex, query });
    require('./sync_elastic')({ knex, query });
    require('./insert_sync_elastic')({ knex, query });
    require('./update_sync_elastic')({ knex, query });
    require('./delete_sync_elastic')({ knex, query });
};
