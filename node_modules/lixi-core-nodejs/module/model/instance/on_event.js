/** lắng nghe sự kiện */

module.exports = client =>
    (client.onEvent = function({ event, handler }) {
        let ev = client.events[event];

        if (!ev) {
            client.events[event] = {
                event,
                handlers: [handler]
            };
        } else {
            ev.handlers.push(handler);
        }
    });
