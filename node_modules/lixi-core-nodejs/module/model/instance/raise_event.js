/** phát sinh sự kiện */

module.exports = client =>
    (client.raiseEvent = function({ event, data }) {
        let ev = client.events[event];
        if (!ev) return;
        for (let handler of ev.handlers) {
            handler(data);
        }
    });
