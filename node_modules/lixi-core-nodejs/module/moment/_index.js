/** thư viện moment, chuyên xử lý date-time */

let Moment = require('moment-timezone');

// đặt mối giờ mặc định: UTC
Moment.tz.setDefault('UTC');

require('./format_db')(Moment);

module.exports = Moment;
