/** mã hóa hex chuỗi uuid
 * @param {string} uuid chuỗi uuid cần mã hóa
 * @returns {buffer} buffer đã mã hóa
 */

module.exports = expo =>
    (expo.hexUuid = function(uuid) {
        return new Buffer.from(uuid.replace(/-/g, ''), 'hex');
    });
