let { Knex } = require('../../index');

test(`select sample`, async () => {
    let db = Knex.init({
        dialect: 'mysql',
        connection: {
            host: '10.39.93.66',
            port: 3309,
            username: 'newfeed',
            password: 'QWegbg*KR=4Yc9=v',
            database: 'newfeed'
        }
    });

    let query = db.query().table('account_point');

    expect(query.toString()).toEqual('select * from `account_point`');
});
