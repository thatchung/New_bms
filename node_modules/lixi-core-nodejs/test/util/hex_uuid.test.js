let { Util } = require('../../index');

test(`hex uuid`, async () => {
    let uuid = '867bbf7b-a3e1-424c-b9ee-0e50cb50cc12';

    let result = Util.hexUuid(uuid);
    let exceptResult = new Buffer.from(uuid.replace(/-/g, ''), 'hex');

    expect(result).toEqual(exceptResult);
});
