let { Util } = require('../../index');

test(`unhex uuid`, async () => {
    let uuid = '867bbf7b-a3e1-424c-b9ee-0e50cb50cc12';
    let buffer = new Buffer.from(uuid.replace(/-/g, ''), 'hex');

    let result = Util.unhexUuid(buffer);

    expect(result).toEqual(uuid);
});
